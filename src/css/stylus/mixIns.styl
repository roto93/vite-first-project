flex(dir=row, jc=flex-start, ai=flex-start)
  display flex
  flex-direction dir
  justify-content jc
  align-items ai

trbl(t=0, r=0, b=0, l=0)
  top t
  right r 
  bottom b
  left l

button(cursor=pointer, user-select=none)
  cursor pointer
  user-select none
  &:active
    filter brightness(0.9)